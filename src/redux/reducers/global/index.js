const initialState = {
  login: false,
  auth: {},
  user: {},
  loading: {},
  products: [],
  product: {},
  users: [],
  profile: {}
}

const globalReducer = (state = initialState, action) => {
  switch (action.type) {
    
  }
  
  return state
}

export default globalReducer